import React from 'react'

const AppLayout: React.FC = ({ children }) => {
    return (
        <div>
            <h1>App Layout: </h1>
            {children}
        </div>
    )
}

export default AppLayout
